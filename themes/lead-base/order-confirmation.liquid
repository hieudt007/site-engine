{% layout "layout" %}
{% block content %}
<div class="text-center py-8">
  <div class="text-4xl">✅</div>
  <h1 class="text-2xl font-bold mt-2">Đặt hàng thành công</h1>
  <p class="text-gray-600">Cảm ơn <strong>{{ order.customerName | escape }}</strong>, chúng tôi đã nhận được đơn hàng và sẽ liên hệ sớm.</p>
  <p class="text-gray-400 text-sm">Mã đơn: {{ order.id }}</p>
</div>

<div class="max-w-md mx-auto border border-gray-200 rounded-lg p-5">
  <h2 class="text-base font-semibold mt-0">Chi tiết đơn hàng</h2>
  {% for item in order.items %}
    <div class="flex justify-between py-2 border-b border-gray-100">
      <span>{{ item.name | escape }} × {{ item.quantity }}</span>
      <span>{{ item.price }}₫</span>
    </div>
  {% endfor %}
  <div class="flex justify-between font-bold mt-3">
    <span>Tổng cộng</span>
    <span>{{ order.total }}₫</span>
  </div>
  <div class="mt-3 text-sm text-gray-500">
    Giao tới: {{ order.customerAddress | default: "-" | escape }}<br>
    SĐT: {{ order.customerPhone | escape }}
  </div>
  {% render "custom-fields", fields: order.customFields %}
</div>

<div class="text-center mt-6">
  <a href="/products" class="text-brand">← Tiếp tục mua sắm</a>
</div>
{% endblock %}
