<article class="variant-{{ variant }}">
    ...
    <a href="{{ productUrlPrefix }}/{{ product.slug | default: product.id }}">{{ product.name }}</a>
    <a class="global-add-to-cart-btn" rel="nofollow" href="/cart/checkout?add_product_id={{ product.id }}" data-product-id="{{ product.id }}">Add</a>
</article>