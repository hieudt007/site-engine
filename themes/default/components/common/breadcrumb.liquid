{% if breadcrumbs and breadcrumbs.size > 0 %}
<nav aria-label="Breadcrumb" class="variant-{{ breadcrumbVariant }}">
    ...
    <ol>
    {% for item in breadcrumbs %}
        <li {% if forloop.last %}aria-current="page"{% endif %}>
            <a href="{{ item.url }}">{{ item.name }}</a>
        </li>
    {% endfor %}
    </ol>
</nav>
{% endif %}