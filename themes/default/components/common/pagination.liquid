{% if hasPrev or hasNext %}
<nav>
    ...
    {% if hasPrev %}
        <a href="{{ basePath }}?page={{ prevPage }}">Prev</a>
    {% endif %}
    {% if hasNext %}
        <a href="{{ basePath }}?page={{ nextPage }}">Next</a>
    {% endif %}
</nav>
{% endif %}