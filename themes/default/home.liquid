{% layout "layout" %}
{% block content %}
<section>
    <h2>...</h2>
    <div>
        {% if products and products.size > 0 %}
            {% for product in products %}
                ...
            {% endfor %}
        {% endif %}
    </div>
</section>
<section>
    <h2>...</h2>
    <div>
        {% if posts and posts.size > 0 %}
            {% for post in posts %}
                ...
            {% endfor %}
        {% endif %}
    </div>
</section>
{% endblock %}