{% if fields.size > 0 %}
<div class="mt-8 border-t border-gray-200 pt-4">
  <h3 class="text-sm font-semibold uppercase tracking-wide text-gray-500 mb-2">Thông tin thêm</h3>
  <dl class="text-sm grid grid-cols-[auto,1fr] gap-x-4 gap-y-1">
    {% for pair in fields %}
      <dt class="text-gray-500">{{ pair[0] | escape }}</dt>
      <dd>{{ pair[1] | escape }}</dd>
    {% endfor %}
  </dl>
</div>
{% endif %}
